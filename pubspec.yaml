name: go_router_poproute_repro
description: "Reproduction for https://github.com/flutter/flutter/issues/188993"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.12.1

dependencies:
  flutter:
    sdk: flutter
  go_router: 17.3.0

# Uncomment to run against the fix from flutter/packages#12111.
# dependency_overrides:
#   go_router:
#     git:
#       url: https://github.com/brotoo25/packages.git
#       ref: go-router-poproute-unmounted-shell-guard
#       path: packages/go_router

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
